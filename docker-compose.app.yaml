version: '3.8'

services:
  backend:
    container_name: backend
    image: valeragug/ava_pro_detealing:latest
    env_file:
      - .env
  
  nginx:
    image: nginx:latest
    ports:
      - 8000:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf